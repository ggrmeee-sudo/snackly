# Snackly

```bash
npm install
npm run dev:full    # разработка
npm run build       # → frontend/dist
npm start           # Express + dist
npm run bot:telegram
```

`.env` в корне по шаблону `backend/env.example`.
